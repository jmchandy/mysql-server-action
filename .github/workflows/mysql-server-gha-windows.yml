name: Build and test mysql-server on windows 

on:
  pull_request:
    branches: [ "trunk" ]

jobs:
  windows-build:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - name: Git setup
        shell: pwsh
        run: git config --system core.longpaths true

      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Analyze changed files
        id: diff
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'

          $baseSha = '${{ github.event.pull_request.base.sha }}'
          $headSha = '${{ github.event.pull_request.head.sha }}'

          $changedFiles = @(git diff --name-only $baseSha $headSha | Where-Object { $_ -and $_.Trim() })

          Write-Host '================================='
          Write-Host 'Changed files detected in PR:'
          Write-Host '================================='

          if ($changedFiles.Count -eq 0) {
            Write-Host '(none)'
          } else {
            $changedFiles | ForEach-Object { Write-Host $_ }
          }

          Write-Host '================================='

          $hasInnodbChanges = $false

          if ($changedFiles.Count -gt 0) {
            if ($changedFiles | Where-Object { $_ -match '(^|/)(innodb|innobase)(/|$)' }) {
              $hasInnodbChanges = $true
            }
          }

          "has_innodb_changes=$hasInnodbChanges" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append

      - name: Install OpenSSL
        id: openssl
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'

          choco install openssl.light -y --no-progress

          $opensslDir = Get-ChildItem "C:\Program Files","C:\Program Files (x86)" -Directory -Filter "OpenSSL*" |
            Select-Object -First 1

          if ($null -eq $opensslDir) {
            throw "OpenSSL installation directory not found"
          }

          $opensslRoot = $opensslDir.FullName -replace '\\', '/'

          Write-Host "Using OpenSSL root: $opensslRoot"
          & "$($opensslDir.FullName)\bin\openssl.exe" version

          "openssl_root=$opensslRoot" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
      
      - name: Install Bison
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          choco install winflexbison3 -y --no-progress
          where.exe win_bison
          win_bison --version  
          
      - name: Build
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'

          if (Test-Path bld) { Remove-Item -Recurse -Force bld }
          New-Item -ItemType Directory -Path bld | Out-Null
          Set-Location bld

          $cmake = 'C:\Program Files\CMake\bin\cmake.exe'
          $args = @(
            '-G', 'Visual Studio 17 2022'
            '-DWITH_BOOST=C:/boost'
            '-DWITH_SSL=C:/Program Files/OpenSSL'
            '-DBISON_EXECUTABLE=C:/ProgramData/chocolatey/bin/win_bison.exe'
            '..'
          )

          & $cmake @args
          & $cmake --build . -- /m
          
      - name: Create package
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'

          Set-Location bld
          & 'C:\Program Files\CMake\bin\cmake.exe' --build . --target package -- /m

      - name: Run MTR
        shell: pwsh
        continue-on-error: true
        run: |
          $ErrorActionPreference = 'Stop'

          $mysqlTestDir = Join-Path $env:GITHUB_WORKSPACE 'bld\mysql-test'
          $summaryReport = Join-Path $env:GITHUB_WORKSPACE ("mtr-{0}.txt" -f $env:GITHUB_RUN_NUMBER)
          $xmlReport = Join-Path $env:GITHUB_WORKSPACE 'mtr.xml'
          $vardir = Join-Path $env:RUNNER_TEMP 'var-dir'

          if (Test-Path $vardir) {
            Remove-Item -Recurse -Force $vardir
          }
          New-Item -ItemType Directory -Path $vardir | Out-Null

          Set-Location $mysqlTestDir

          if ("${{ steps.diff.outputs.has_innodb_changes }}" -eq "true") {
            perl mysql-test-run.pl `
              --vardir="$vardir" `
              --force `
              --retry-failure=2 `
              --retry=2 `
              --max-test-fail=0 `
              --report-unstable-tests `
              --clean-vardir `
              --xml-report="$xmlReport" `
              --parallel=$env:NUMBER_OF_PROCESSORS `
              --suite=innodb `
              --summary-report="$summaryReport"
          }
          else {
            perl mysql-test-run.pl `
              --vardir="$vardir" `
              --force `
              --retry-failure=2 `
              --retry=2 `
              --max-test-fail=0 `
              --report-unstable-tests `
              --clean-vardir `
              --xml-report="$xmlReport" `
              --parallel=$env:NUMBER_OF_PROCESSORS `
              --summary-report="$summaryReport" `
              1st
          }

      - name: Cleanup
        if: always()
        shell: pwsh
        run: |
          if (Test-Path bld) {
            Remove-Item -Recurse -Force bld
          }

          if (Test-Path mtr.xml) {
            Remove-Item -Force mtr.xml
          }
